{
  "name": "hariomlohardev.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "Hariom Lohar — Lab Notebook No.01 · hariomlohardev.github.io — static site, builds via Node scripts, ships to GitHub Pages and Vercel",
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node scripts/make-favicon-ico.js && node scripts/generate-blog.js && node scripts/generate-projects.js && node scripts/generate-llms.js && node scripts/generate-opensource.js && node scripts/generate-tricks.js && node scripts/prerender-lists.js && node scripts/rasterize-og.js",
    "vercel-build": "npm run build",
    "dev": "python -m http.server 8000",
    "generate:blog": "node scripts/generate-blog.js",
    "generate:projects": "node scripts/generate-projects.js",
    "generate:llms": "node scripts/generate-llms.js",
    "generate:opensource": "node scripts/generate-opensource.js",
    "generate:tricks": "node scripts/generate-tricks.js",
    "generate:prerender": "node scripts/prerender-lists.js",
    "generate:og": "node scripts/generate-site-og.js && node scripts/rasterize-og.js",
    "generate:favicon": "node scripts/make-favicon-ico.js",
    "check": "node --check scripts/generate-blog.js && node --check scripts/generate-projects.js && node --check scripts/generate-tricks.js && node --check scripts/rasterize-og.js && node --check scripts/generate-llms.js && node --check scripts/prerender-lists.js && node --check scripts/make-favicon-ico.js && node scripts/check-vercel-json.js && echo 'ok'"
  },
  "keywords": [
    "hariomlohardev",
    "Hariom Lohar",
    "CS50P",
    "AGI",
    "portfolio"
  ],
  "author": "Hariom Lohar <hariomlohar.new@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hariomlohardev/hariomlohardev.github.io"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "bcryptjs": "^2.4.3",
    "cookie": "^0.6.0",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.21.3"
  }
}
