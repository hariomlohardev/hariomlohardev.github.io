# SEO Checklist — Hariom Lohar · hariomlohardev.github.io

Keep this green — all $0, Lab Notebook No.01.

## Entity: own `hariom lohar` + `hariom lohar github`
- [x] Titles front-load `Hariom Lohar` (index, blog, post, projects)
- [x] `@graph` Person+WebSite+WebPage+Breadcrumb+FAQ + BlogPosting per post
- [x] `rel=me` + `sameAs` GitHub/X/LinkedIn
- [x] Visible FAQ matching query (“Who is Hariom Lohar…”, “Where is GitHub…”)
- [x] `og:image` per post — now `og/<slug>.svg` (Lab Notebook) or `cover` frontmatter; fallback cert 2246×1588
- [x] `sitemap.xml` + `robots.txt` + `humans.txt` + `site.webmanifest` + `.nojekyll`
- [ ] **You do once:** GitHub bio → link `https://hariomlohardev.github.io/` ; LinkedIn featured → same ; X bio → same (closes entity loop)
- [ ] **After next push:** Search Console → Sitemaps → `https://hariomlohardev.github.io/sitemap.xml` → Request Indexing for `/` `/blog.html` `/blog/p/*/`
- [ ] Wait 24–72h `site:hariomlohardev.github.io`, 3–7 days name query (you already added sitemap)

## Per-post
- frontmatter `title` 30–60 chars front-load, `description` ≤155, `cover` optional (e.g. `cover: /og/my-cover.png` or absolute URL)
- `cover` → used for `og:image`+`twitter:image`+JSON-LD `image`; else `og/<slug>.svg` auto-generated by `node scripts/generate-blog.js`
- Body: one H1 via `title`, start markdown at `##`, 2–4 tags (`daily-log` for logs)

## Build
- `node scripts/generate-blog.js` → `posts.json` + `feed.xml` + `blog/p/*/index.html` + `og/*.svg` + patch `sitemap.xml`
- Workflow `.github/workflows/pages.yml` runs it before `upload-pages-artifact` (Node 20, free)

## Verify after push
```
curl -I https://hariomlohardev.github.io/sitemap.xml
curl -I https://hariomlohardev.github.io/feed.xml
curl -s https://hariomlohardev.github.io/blog/p/hello-daily-logs-day-001/ | grep -i "og:image"
curl -s https://hariomlohardev.github.io/og/hello-daily-logs-day-001.svg | head -n 1
```
